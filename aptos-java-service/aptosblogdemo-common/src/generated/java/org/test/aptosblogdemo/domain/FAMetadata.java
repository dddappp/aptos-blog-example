// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosblogdemo.domain;

import java.io.Serializable;
import org.test.aptosblogdemo.domain.*;

public class FAMetadata implements Serializable {
    private String name;

    public String getName()
    {
        return this.name;
    }

    public void setName(String name)
    {
        this.name = name;
    }

    private String symbol;

    public String getSymbol()
    {
        return this.symbol;
    }

    public void setSymbol(String symbol)
    {
        this.symbol = symbol;
    }

    private Integer decimals;

    public Integer getDecimals()
    {
        return this.decimals;
    }

    public void setDecimals(Integer decimals)
    {
        this.decimals = decimals;
    }

    private String iconUri;

    public String getIconUri()
    {
        return this.iconUri;
    }

    public void setIconUri(String iconUri)
    {
        this.iconUri = iconUri;
    }

    private String projectUri;

    public String getProjectUri()
    {
        return this.projectUri;
    }

    public void setProjectUri(String projectUri)
    {
        this.projectUri = projectUri;
    }

    public FAMetadata()
    {
    }

    public FAMetadata(String name, String symbol, Integer decimals, String iconUri, String projectUri)
    {
        this.name = name;
        this.symbol = symbol;
        this.decimals = decimals;
        this.iconUri = iconUri;
        this.projectUri = projectUri;
    }

    @Override
    public boolean equals(Object obj)
    {
        if (obj == this) {
            return true;
        }
        if (obj == null || obj.getClass() != this.getClass()) {
            return false;
        }

        FAMetadata other = (FAMetadata)obj;
        return true 
            && (name == other.name || (name != null && name.equals(other.name)))
            && (symbol == other.symbol || (symbol != null && symbol.equals(other.symbol)))
            && (decimals == other.decimals || (decimals != null && decimals.equals(other.decimals)))
            && (iconUri == other.iconUri || (iconUri != null && iconUri.equals(other.iconUri)))
            && (projectUri == other.projectUri || (projectUri != null && projectUri.equals(other.projectUri)))
            ;
    }

    @Override
    public int hashCode()
    {
        int hash = 0;
        if (this.name != null) {
            hash += 13 * this.name.hashCode();
        }
        if (this.symbol != null) {
            hash += 13 * this.symbol.hashCode();
        }
        if (this.decimals != null) {
            hash += 13 * this.decimals.hashCode();
        }
        if (this.iconUri != null) {
            hash += 13 * this.iconUri.hashCode();
        }
        if (this.projectUri != null) {
            hash += 13 * this.projectUri.hashCode();
        }
        return hash;
    }

    @Override
    public String toString() {
        return "FAMetadata{" +
                "name=" + '\'' + name + '\'' +
                ", symbol=" + '\'' + symbol + '\'' +
                ", decimals=" + decimals +
                ", iconUri=" + '\'' + iconUri + '\'' +
                ", projectUri=" + '\'' + projectUri + '\'' +
                '}';
    }


}

