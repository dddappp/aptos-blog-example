// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosblogdemo.domain;

import java.io.Serializable;
import java.math.BigInteger;
import org.test.aptosblogdemo.domain.*;

public class FungibleStore implements Serializable {
    private String metadata;

    public String getMetadata()
    {
        return this.metadata;
    }

    public void setMetadata(String metadata)
    {
        this.metadata = metadata;
    }

    private BigInteger balance;

    public BigInteger getBalance()
    {
        return this.balance;
    }

    public void setBalance(BigInteger balance)
    {
        this.balance = balance;
    }

    private Boolean frozen;

    public Boolean getFrozen()
    {
        return this.frozen;
    }

    public void setFrozen(Boolean frozen)
    {
        this.frozen = frozen;
    }

    public FungibleStore()
    {
    }

    public FungibleStore(String metadata, BigInteger balance, Boolean frozen)
    {
        this.metadata = metadata;
        this.balance = balance;
        this.frozen = frozen;
    }

    @Override
    public boolean equals(Object obj)
    {
        if (obj == this) {
            return true;
        }
        if (obj == null || obj.getClass() != this.getClass()) {
            return false;
        }

        FungibleStore other = (FungibleStore)obj;
        return true 
            && (metadata == other.metadata || (metadata != null && metadata.equals(other.metadata)))
            && (balance == other.balance || (balance != null && balance.equals(other.balance)))
            && (frozen == other.frozen || (frozen != null && frozen.equals(other.frozen)))
            ;
    }

    @Override
    public int hashCode()
    {
        int hash = 0;
        if (this.metadata != null) {
            hash += 13 * this.metadata.hashCode();
        }
        if (this.balance != null) {
            hash += 13 * this.balance.hashCode();
        }
        if (this.frozen != null) {
            hash += 13 * this.frozen.hashCode();
        }
        return hash;
    }

    @Override
    public String toString() {
        return "FungibleStore{" +
                "metadata=" + '\'' + metadata + '\'' +
                ", balance=" + balance +
                ", frozen=" + frozen +
                '}';
    }


}

